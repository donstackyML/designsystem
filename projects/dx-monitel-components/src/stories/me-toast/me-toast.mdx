import { Canvas, Controls, Meta, Source, Story } from "@storybook/addon-docs";
import { moduleMetadata } from "@storybook/angular";
import { DxButtonModule, DxToastModule } from "devextreme-angular";
import { MeButtonDirective, MeButtonModule, MeIconComponent, MeToastDirective } from "../../public-api";
import * as toastStories from "./me-toast.stories";

<Meta
  title="Components/Toast"
  of={toastStories}
  decorators={[
    moduleMetadata({
      declarations: [MeToastDirective],
      imports: [DxToastModule, DxButtonModule, MeButtonModule, MeIconComponent],
    }),
    ]}
/>

# Toast

## Описание

Компонент `Toast` используется для отображения уведомлений различных типов: информационных сообщений, предупреждений, ошибок и уведомлений об успешных операциях.

## Размеры и типы

Toast поддерживает два размера:
- Маленький (`small`) - для простых уведомлений
- Большой (`large`) - для уведомлений с дополнительными действиями

И пять типов:
- Информационный (`info`)
- Предупреждение (`warning`)
- Ошибка (`error`)
- Успех (`success`)
- Информационный инвертированный (`info-inverted`)

## Тост обычный с информацией

<Source
  code={`<dx-toast
    meToast
    [visible]="visible"
    type="info"
    message="Операция выполнена успешно"
  >
  </dx-toast>`}
  dark
  language="html"
/>

## Тост расширенный с информацией

<Source
  code={`<me-toast
    [visible]="visible"
    type="info"
    message="Операция выполнена успешно"
    title="Информация"
  >
  </me-toast>`}
  dark
  language="html"
/>

## API

### Свойства компонента

| Свойство | Тип | Описание |
|----------|-----|----------|
| type | 'info' \| 'warning' \| 'error' \| 'success' \| 'info-inverted' | Тип уведомления |
| visible | boolean | Флаг видимости уведомления |
| displayTime | number | Время отображения в миллисекундах |

### CSS классы

| Класс | Описание |
|-------|----------|
| me-toast-content | Базовый класс для контейнера уведомления |
| me-toast-small | Класс для маленького уведомления |
| me-toast-large | Класс для большого уведомления |
| me-toast-header | Класс для заголовка уведомления |
| me-toast-content1 | Класс для отступа контента в больших уведомлениях |
| me-toast-content2 | Класс для отступа контента в маленьких уведомлениях |
| me-toast-actions | Класс для контейнера с кнопками действий |
| me-toast-close-button | Класс для кнопки закрытия |
