import { Meta, Story, Controls, Source } from "@storybook/addon-docs";
import * as IconStories from "./me-icon.stories";
import { MeIconComponent } from "../../public-api";
import { moduleMetadata } from "@storybook/angular";
import { iconOptions } from "./me-icon.stories";

<Meta
  title="Компоненты/Icon"
  component={MeIconComponent}
  decorators={[
    moduleMetadata({
      imports: [MeIconComponent],
    }),
    ]}
  of={IconStories}
/>

# MeIcon Component

## Примеры использования:

### Иконка по умолчанию

<Story name="Default" of={IconStories.Default} />

<Controls of={IconStories.Default} />

<Source of={IconStories.Default} dark />

### Список всех доступных иконок

Чтобы выбрать иконку, вы можете использовать любой из следующих вариантов:

<Source of={IconStories.AllIcons} dark />
