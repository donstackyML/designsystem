import { Meta, Story, Controls, Source, Canvas } from "@storybook/addon-docs";
import * as textBoxStories from "./me-text-box.stories";
import { Figma } from "@storybook/addon-designs/blocks";

<Meta name="TextBox Documentation" of={textBoxStories} />

# TextBox

## Директива meTextBox, meLabel

Применение директивы <code>meTextBox</code> к элементу <code>dx-text-box</code>
приводит его размеры к значению <code>size=medium (32)</code> из дизайн-системы, задает по умолчанию <code>stylingMode=filled</code> и предоставляет доступ к другим свойствам.

[API](https://js.devexpress.com/Angular/Documentation/ApiReference/UI_Components/dxTextBox/)

[Демо](https://js.devexpress.com/Angular/Demos/WidgetsGallery/Demo/TextBox/Overview/Light/)

<Canvas of={textBoxStories.Default} />

<Controls of={textBoxStories.Default} />

## Дизайн

<Figma
  height="50%"
  collapsable
  defaultCollapsed
  url="https://www.figma.com/design/S2KXryEyWLA9cplaicYrVn/Components?node-id=17-1263&t=gC6C40DcbGAFZEUK-0"
/>

## Примеры использования:

- [Определение положения лейбла снаружи текстового поля](#определение-положения-лейбла-снаружи-текстового-поля)
- [Положение лейбла сверху](#положение-лейбла-сверху)
- [Положение лейбла слева](#положение-лейбла-слева)
- [Использование масок в текстовом поле](#использование-масок-в-текстовом-поле)
- [Показ и скрытие пароля](#показ-и-скрытие-пароля)

## Определение положения лейбла снаружи текстового поля

Применение директивы <code>meLabel</code> позволяет определить положение лейбла снаружи текстового поля (сверху или слева).

<code>labelDirection</code> определяет положение лейбла снаружи текстового поля (сверху
или слева).

### Положение лейбла сверху

Для положения `label` сверху используйте тэг <code>label</code> с директивой <code>meLabel</code> и установите параметр <code>labelDirection="column"</code>. Также установите необходимые стили выравнивания как <code>align-items: flex-start</code>.

<Canvas of={textBoxStories.WithLabelColumn} />

### Положение лейбла слева

Для положения `label` слева установите параметр <code>labelDirection="row"</code>.

<Canvas of={textBoxStories.WithLabelRow} />

<code>width</code> определяет размер текстового поля вместе с лейблом (используется
в теге <code>label</code> для расположения <code>labelPosition="left"</code>)

<style>
{`
    h2 {
      border-bottom: none !important;
    }
`}
</style>

`Description` добавляется самостоятельно разработчиком, так как `DevExtreme` не поддерживает данный элемент.

### Использование масок в текстовом поле

Для маски, например телефона, используйте параметр <code>mask</code> в виде строки `"+7 (000) 000-0000"`.

<Canvas of={textBoxStories.WithTelMask} />

### Показ и скрытие пароля

Для показа и скрытия пароля используется параметр <code>mode='password'</code>.

<Canvas of={textBoxStories.WithPasswordToggle} />
