import { Meta, Story, Controls, Source, Canvas } from "@storybook/addon-docs";
import * as contextmenuStories from "./me-context-menu.stories";
import { Figma } from "@storybook/addon-designs/blocks";

<Meta name="Context Menu Documentation" of={contextmenuStories} />

# Context Menu

## Директива meContextMenu

Применение директивы <code>meContextMenu</code> к элементу <code>dx-context-menu</code> приводит его внешний вид в соответствие с дизайн-системой.

[Ссылка на API компонента DevExtreme](https://js.devexpress.com/Angular/Documentation/ApiReference/UI_Components/dxContextMenu/)

[Ссылка на Demo компонента DevExtreme](https://js.devexpress.com/Angular/Demos/WidgetsGallery/Demo/ContextMenu/Basics/MaterialBlueLight/)

<Canvas of={contextmenuStories.Default} />
<Controls of={contextmenuStories.Default} />
<Source of={contextmenuStories.Default} dark />

## Дизайн

<Figma
  height="50%"
  collapsable
  defaultCollapsed
  url="https://www.figma.com/design/S2KXryEyWLA9cplaicYrVn/Components?node-id=745-25563&node-type=canvas&t=1Amx2NO5kVyJu99X-0"
/>

## Примеры использования

## Добавление заголовка в контекстное меню

В DevExtreme для компонента Context Menu заголовок не предусмотрен.
Предлагается для добавления заголовка стилизовать один из пунктов меню с помощью шаблона <code>menu-title</code>. Например,

<Source
  code={`
  contextMenuItems: dxContextMenuItem[] = [
    {
      text: 'Заголовок',
      icon: this.iconService.getIcon({ icon: 'public', size: '24' }),
      disabled: true,
      beginGroup: true
    },
    // ...остальные элементы
    
`}
  dark
  language="js"
/>

Обратите внимание, для того чтобы отключить стандартные состояния <code>(hover, active)</code>
заголовку добавлено свойство <code>disabled</code> со значением <code>true</code>. Для добавления разделителя групп используйте `beginGroup: true`

## Состояние `selected`

Состояние `selected` в контекстном меню устанавливается атрибутом <code>selected: true</code> в `dataSource`.

<Source
  code={`
          { text: 'Add to Favorite', selected: true }
`}
  dark
  language="js"
/>
