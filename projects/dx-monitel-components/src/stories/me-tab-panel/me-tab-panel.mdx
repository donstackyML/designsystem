import { Meta, Story, Canvas, Controls, Source } from "@storybook/addon-docs";
import * as TabPanelStories from "./me-tab-panel.stories";

<Meta title="Components/TabPanel" of={TabPanelStories} />

# TabPanel

Компонент TabPanel представляет собой контейнер для отображения содержимого во вкладках.

<Canvas of={TabPanelStories.Default} />

## Свойства

<Controls />

## Использование

Для использования TabPanel в вашем приложении, примените директиву `meTabPanel` к компоненту `dx-tab-panel`:

<Source
  language="html"
  dark
  code={`
<dx-tab-panel
  meTabPanel
  [customClass]="customClass"
  [selectedIndex]="selectedIndex"
  [animationEnabled]="animationEnabled"
  [swipeEnabled]="swipeEnabled"
  [dataSource]="tabPanelData"
  [orientation]="orientation"
  [stylingMode]="stylingMode"
  [iconPosition]="iconPosition"
  [showNavButtons]="showNavButtons"
  [scrollByContent]="scrollByContent"
  [rtlEnabled]="rtlEnabled"
  [width]="width"
>
</dx-tab-panel>
  `}
/>

## Примеры

### Вертикальная ориентация

<Canvas of={TabPanelStories.VerticalOrientation} />

### С кнопками навигации

<Canvas of={TabPanelStories.WithNavButtons} />

### Стиль оформления "Outside"

<Canvas of={TabPanelStories.OutsideStylingMode} />

### Различные позиции иконок

<Canvas of={TabPanelStories.DifferentIconPositions} />

### Поддержка языков с письмом справа налево

<Canvas of={TabPanelStories.RightToLeft} />

## API

<Controls
  of={TabPanelStories.Default}
  include={[
    "customClass",
    "selectedIndex",
    "animationEnabled",
    "swipeEnabled",
    "position",
    "size",
    "stylingMode",
    "orientation",
    "iconPosition",
    "showNavButtons",
    "scrollByContent",
    "rtlEnabled",
    "width",
  ]}
/>
